[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[edge_functions]]
  function = "gemini-stream"
  path = "/api/gemini-stream"

# El redirect SPA debe ser el último y no debe forzar rutas de API
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
